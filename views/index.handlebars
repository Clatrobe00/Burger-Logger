<h1>Burgers</h1>

<h2>Burgers to be eaten:</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  <li>
	  {{burger_name}}

	    <button class="eat" id="{{id}}" devoured="{{devoured}}">Eat!</button>
</li>
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
  <li>
	  {{burger_name}}
  </li>
    {{/if}}
  {{/each}}
</ul>


<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="newBurg">Burger Name:</label>
    <input type="text" id="newBurg" name="name">
  </div>

  <button class= "newBurger" type="submit">Add Burger</button>
</form>